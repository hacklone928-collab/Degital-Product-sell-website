{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "User account information and roles",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "role"]
    },
    "Product": {
      "title": "Digital Product",
      "description": "Metadata for software, plugins, or scripts",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" },
        "price": { "type": "number" },
        "category": { "type": "string" },
        "tags": { "type": "array" },
        "imageUrl": { "type": "string" },
        "fileUrl": { "type": "string", "description": "Private link or storage path" },
        "fileName": { "type": "string" },
        "licenseType": { "type": "string", "enum": ["single", "multiple"] },
        "rating": { "type": "number" },
        "reviewCount": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "price", "category"]
    },
    "Order": {
      "title": "Transaction Order",
      "description": "Details of a product purchase",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "productId": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "completed", "failed"] },
        "amount": { "type": "number" },
        "currency": { "type": "string" },
        "paymentIntentId": { "type": "string" },
        "downloadToken": { "type": "string" },
        "licenseKey": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "productId", "status", "amount"]
    },
    "Settings": {
      "title": "Site Settings",
      "description": "Global website configuration",
      "type": "object",
      "properties": {
        "siteName": { "type": "string" },
        "heroTitle": { "type": "string" },
        "heroSubtitle": { "type": "string" },
        "footerText": { "type": "string" },
        "logoUrl": { "type": "string" },
        "faviconUrl": { "type": "string" }
      },
      "required": ["siteName"]
    }
  },
  "firestore": {
    "/settings/site": {
      "schema": "Settings",
      "description": "Global site configuration settings"
    },
    "/users/{userId}": {
      "schema": "User",
      "description": "Individual user profiles"
    },
    "/products/{productId}": {
      "schema": "Product",
      "description": "Public catalog of digital items"
    },
    "/orders/{orderId}": {
      "schema": "Order",
      "description": "Purchase records"
    }
  }
}
